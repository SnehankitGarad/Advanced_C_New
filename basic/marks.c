#include<stdio.h>
int main(){
	int marks;
	char gender;
	float height;
	double avg_marks;

	printf("enter the marks:");
	scanf("%d",&marks);
	printf("enter the gender:");
	scanf(" %c",&gender);
        printf("enter the height:");
	scanf("%f",&height);
        printf("enter the avg marks:");
	scanf("%lf",&avg_marks);
}
	
